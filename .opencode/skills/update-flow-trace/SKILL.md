---
name: update-flow-trace
description:
  Update agent/ harness docs (flow-trace, invariants, context) to match the current branch's code
  changes. Use after changing contracts, circuits, actors, or CLI behavior.
---

Read `agent/prompts/update-flow-trace.md` and follow it exactly. This file is an OpenCode adapter
only; the canonical procedure lives in `agent/prompts/`.
